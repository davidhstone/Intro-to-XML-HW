XML Homework

1) How many elements does this xml file have? 8

2) What is a namespace in this example? 

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="generalassembly.yuliyakaleda.supportdifferentdevices">

3) Name parent and children elements.

L to R is going parent to children:  
     Parent: manifest -> application -> service ->
     				     application -> activity -> intent filter -> action	
     				     						 -> intent filter -> category
     				     application -> activity

4) Find and name empty elements (the ones that do not have content).

		action, category, activity


WRITE AN XML FILE IS IN A SEPERATE DOC IN THIS FOLDER

